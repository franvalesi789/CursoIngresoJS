function mostrar()
{
	//Genero el número RANDOM entre 1 y 10 
	
	var maximo = 10;
	var minimo = 1;

	var numRandom = Math.round(Math.random() * (maximo - minimo));

	alert(numRandom);

}//FIN DE LA FUNCIÓN